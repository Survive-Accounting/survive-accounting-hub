.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Build / deploy output
.vercel/
supabase/.temp/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# internal context doc containing live secrets — never commit
CONTEXT_SCRAPER_SESSION.md

# SEC roster source CSV (contact data — not committed)…
data/*
# …but JE scenario source docs ARE versioned (the importer reads them from the repo)
!data/scenarios/
# .env.local is created by `vercel link` / `vercel env pull` and carries pulled
# values. The committed rule above is exactly ".env", which does NOT match it —
# so widen it. (.vercel/ is already covered further up.)
.env*
scratch/

# Greek 990 pilot: internal logs/dumps (keep deliverable CSVs/MD)
greek-990-output/_*
