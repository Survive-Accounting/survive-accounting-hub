.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Build / deploy output
.vercel/
supabase/.temp/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# internal context doc containing live secrets — never commit
CONTEXT_SCRAPER_SESSION.md

# SEC roster source CSV (contact data — not committed)…
data/*
# …but JE scenario source docs ARE versioned (the importer reads them from the repo)
!data/scenarios/
# .env.local is created by `vercel link` / `vercel env pull` and carries pulled
# values. The committed rule above is exactly ".env", which does NOT match it —
# so widen it. (.vercel/ is already covered further up.)
.env*
scratch/

# Growth Contact Intelligence runtime artifacts
scripts/growth-intel/.checkpoint.json
scripts/growth-intel/*.log

# Growth Contact Intelligence sample exports (scraped contact data — deliverables, not code)
/COUNCIL_CONTACT_SAMPLE.csv
/GREEK_CHAPTER_CONTACT_SAMPLE.csv
/BUSINESS_CLUB_SAMPLE.csv
scripts/growth-intel/campuses-all.json
/MORNING_AUDIT_GROWTH_CONTACTS.json
/MORNING_AUDIT_GROWTH_CONTACTS.md
