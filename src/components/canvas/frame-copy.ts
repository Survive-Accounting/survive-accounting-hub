// FRAME COPY — the fixed strings that get FILMED into a frame.
//
// One constant, one place. Anything in here ends up baked into footage, so
// changing it later must never mean editing frames one by one.
//
// THE RULE THAT SHAPES THIS FILE: nothing school-specific, exam-specific or
// topic-specific may be filmed. Exam number, university and professor are
// stamped by the HTML player at watch time — footage that names a school is
// footage that can only ever be sold to that school.

/** The eyebrow above the quoted question on every NOTE frame. Static by design:
 *  the same line on every note frame, every topic, every exam. */
export const NOTE_EYEBROW = "FOUND ON YOUR EXAM";
