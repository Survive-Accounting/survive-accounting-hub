You are building ONE change, unattended, in this worktree of the Survive Accounting repo (survive-accounting-hub — TanStack Start, React 19, TypeScript, Supabase, Bun). You are on branch queue/teleprompter-mirror-with-banked-phrases--0myo8, cut from origin/main. Nobody is watching; the owner (Lee) will test the result later from a checklist you write. Read CLAUDE.md and docs/SESSION-CONTEXT.md first.

HARD RULES
- Additive only. New files, new routes, new fields, new tables via a numbered additive migration FILE under migration/supabase-migrations/ — never run it; list it under SQL LEE MUST RUN.
- Never push. Never touch main. Commit your finished work to THIS branch with a clear message (the runner pushes the branch).
- Never delete or weaken a passing test. Run `bunx tsc --noEmit` (the known error in partner-kit.server.ts is pre-existing — ignore it) and `bun test` before you finish; fix what you broke.
- Protected zones (element/frame parent membership, scene serialization internals, command bus, space walk) are off limits. If the task needs them, STOP: make no change there and say so in the REPORT.
- Nothing a student sees changes unless the task says so. No data rewriting.
- Fail loud: no silent fallbacks, no stubs that pretend to work. Two failed attempts on an item → stub it LOUDLY, log it, move on.
- Make it testable by a non-developer: if testing needs data or a mock (a test chapter, a test checkout, a sample student), build a safe test path behind an is_test flag or a clearly-named test route, and put the exact clicks in the checklist.

- COMMIT AS YOU GO. After each working step, `git add -A && git commit` on this branch. If you run out of time or turns, committed work survives; uncommitted work does not.
- SCOPE: if the task reads as a research project or a list of many features, build the SMALLEST complete, testable slice first, commit it, and say in the REPORT what you left for a later pass. A finished small thing beats an unfinished big one.

END YOUR FINAL MESSAGE WITH EXACTLY THESE TWO SECTIONS, nothing after them:
## REPORT
Per item: pass / fail / stubbed. SQL LEE MUST RUN (file names, or 'none'). Anything ambiguous you decided and how. What you could not verify.
## TESTING CHECKLIST
5 to 10 lines. Each line: `- [ ] <one plain-English check a non-developer can do, naming what to click and what they should see> — /the/route/path`. EVERY line ends with ` — /route` (the path on the site where the check happens; the runner turns it into a full preview URL). No jargon, no dev tools, no terminal. If a check needs a test login or a mock, say exactly what to use.

THE TASK
## TLDR

Lee gets a mirrored teleprompter window showing only the yellow-highlighted "say it" phrases in big text, advanced with Enter, for hands-free vertical filming runs.

## Summary

Lee wants a teleprompter that mirrors the capture window for vertical filming, displaying banked phrases he's marked as "say it" (left-click-highlight, yellow). The teleprompter shows these phrases large and advances with Enter (next), Shift+Enter (previous), backtick (reset to top). This ships before Lee's Easy Points filming run where he'll crank out dozens of short videos. Shift-highlight for "show this" visuals (blue) is recognized but stays out of the teleprompter.

## Prompt

**Add teleprompter mirror window with banked phrase navigation for vertical capture**

**CONTEXT**  
The results board at `/v3/easy-points/internal-vs-external-users/blast-off/results` where Lee highlights script lines. Left-click-highlight means "say it" (yellow, banked phrase), Shift-highlight means "show this" (blue, visual). The capture window exists; this adds a mirrored teleprompter showing only the yellow-banked phrases.

**WHAT TO BUILD**  
1. Detect left-click-highlight on script lines in results board — mark as "say it" (yellow background), add to banked phrases array in order of highlight.
2. Detect Shift-highlight — mark as "show this" (blue background), do NOT bank.
3. Add `/v3/teleprompter` route that mirrors the active capture window session — show only yellow-banked phrases, one at a time, centered, large text (4rem+ font size), black text on white.
4. Index state: current phrase index (default 0). Enter → advance index by 1 (wrap to 0 at end). Shift+Enter → decrement index by 1 (wrap to last at -1). Backtick → reset index to 0.
5. Teleprompter displays `phrases[currentIndex]` — if no phrases banked, show "No phrases banked yet" in smaller text.
6. Both windows (capture and teleprompter) see the same banked phrases array in real-time — when Lee highlights a new line on results board, it appears at the end of the teleprompter's phrase list; index does not auto-advance.

**WHERE**  
Results board: likely `app/routes/v3/easy-points/$category/$set/results.tsx` or nearby results component.  
Teleprompter route: new file `app/routes/v3/teleprompter.tsx`.  
Shared state for banked phrases: find existing capture/session store or add Supabase real-time subscription for phrase sync across windows (Lee's build machine and filming laptop both poll or subscribe).

**ACCEPTANCE**  
- Left-click-highlight on results board → line turns yellow, phrase banks.  
- Shift-highlight → line turns blue, does NOT bank.  
- `/v3/teleprompter` opens, shows first banked phrase large and centered.  
- Enter advances, Shift+Enter goes back, backtick resets to top, all wrap correctly.  
- Highlighting a new line on results board → teleprompter sees it in the list immediately (next Enter will reach it).  
- No banked phrases → teleprompter shows placeholder text.

**OUT OF SCOPE**  
- Auto-advancing teleprompter on timer (Lee uses Enter manually).  
- Editing or reordering banked phrases (highlight order is final).  
- Unhighlighting or removing phrases (additive only).  
- Visual/blue phrases in teleprompter (they stay on results board only).

**REPORT**  
Per item: pass/fail/stubbed.  

**SQL LEE MUST RUN**  
(List numbered migrations if phrase persistence needs a table; if in-memory session store suffices, state "none — session state only, no DB.")

Anything ambiguous:  
- Phrase sync method (polling vs. Supabase real-time) — chose based on existing capture window session pattern; state which.  
- Font size — chose 4rem, confirm readable at Lee's filming distance or leave configurable in code comment.  
- Wrap behavior — chose wrap-around (after last phrase, Enter goes to first); confirm or flag if Lee wants hard stop.

## Testing checklist

- [ ] Open `/v3/easy-points/internal-vs-external-users/blast-off/results`, left-click-highlight three script lines → all turn yellow.
- [ ] Shift-highlight one line → turns blue, does not appear in teleprompter phrase count.
- [ ] Open `/v3/teleprompter` in second browser tab/window → see first yellow phrase large and centered.
- [ ] Press Enter three times → advance through all three yellow phrases, fourth Enter wraps to first.
- [ ] Press Shift+Enter → move back one phrase; at first phrase, Shift+Enter wraps to last.
- [ ] Press backtick → teleprompter resets to first phrase.
- [ ] With teleprompter open, go back to results board and left-click-highlight a new line → teleprompter phrase list updates in under 2 seconds, new phrase reachable with Enter.
- [ ] Open teleprompter before highlighting anything → see "No phrases banked yet" placeholder.