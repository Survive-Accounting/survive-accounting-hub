You are building ONE change, unattended, in this worktree of the Survive Accounting repo (survive-accounting-hub — TanStack Start, React 19, TypeScript, Supabase, Bun). You are on branch queue/control-i-idea-bank-persistent-draggable-flzdv, cut from origin/main. Nobody is watching; the owner (Lee) will test the result later from a checklist you write. Read CLAUDE.md and docs/SESSION-CONTEXT.md first.

HARD RULES
- Additive only. New files, new routes, new fields, new tables via a numbered additive migration FILE under migration/supabase-migrations/ — never run it; list it under SQL LEE MUST RUN.
- Never push. Never touch main. Commit your finished work to THIS branch with a clear message (the runner pushes the branch).
- Never delete or weaken a passing test. Run `bunx tsc --noEmit` (the known error in partner-kit.server.ts is pre-existing — ignore it) and `bun test` before you finish; fix what you broke.
- Protected zones (element/frame parent membership, scene serialization internals, command bus, space walk) are off limits. If the task needs them, STOP: make no change there and say so in the REPORT.
- Nothing a student sees changes unless the task says so. No data rewriting.
- Fail loud: no silent fallbacks, no stubs that pretend to work. Two failed attempts on an item → stub it LOUDLY, log it, move on.
- Make it testable by a non-developer: if testing needs data or a mock (a test chapter, a test checkout, a sample student), build a safe test path behind an is_test flag or a clearly-named test route, and put the exact clicks in the checklist.

- COMMIT AS YOU GO. After each working step, `git add -A && git commit` on this branch. If you run out of time or turns, committed work survives; uncommitted work does not.
- SCOPE: if the task reads as a research project or a list of many features, build the SMALLEST complete, testable slice first, commit it, and say in the REPORT what you left for a later pass. A finished small thing beats an unfinished big one.

END YOUR FINAL MESSAGE WITH EXACTLY THESE TWO SECTIONS, nothing after them:
## REPORT
Per item: pass / fail / stubbed. SQL LEE MUST RUN (file names, or 'none'). Anything ambiguous you decided and how. What you could not verify.
## TESTING CHECKLIST
5 to 10 lines. Each line: `- [ ] <one plain-English check a non-developer can do, naming what to click and what they should see> — /the/route/path`. EVERY line ends with ` — /route` (the path on the site where the check happens; the runner turns it into a full preview URL). No jargon, no dev tools, no terminal. If a check needs a test login or a mock, say exactly what to use.

THE TASK
## TLDR

Control+I idea bank modal becomes persistent, movable, collapsible, with screenshot selector — student loses nothing when clicking away.

## Summary

Lee lost everything when clicking away from the idea submission modal and had to start over. The idea bank modal needs to persist state, become draggable/resizable/collapsible to a bolt icon, sit over content (not blanked-out backdrop) so students can screenshot exactly what they're looking at with a selector tool, and save progress automatically. Categories get simplified: "Authoring/Editing" replaces "Authoring," add "Student Side," add "Other (specify)," and the modal launches with the animated bolt. Lee wants the full submission to optionally split into phased prompts Claude Code can ask about.

## Prompt

**Control+I idea bank: persistent modal with screenshot selector**

**CONTEXT**  
`/app/routes/v3.easy-points.internal-vs-external-users.blast-off.talkthrough.tsx` — the 🎙 Talkthrough — Blast Off page — launches the Control+I idea bank modal. Currently loses all entered data when dismissed; blanks out the background; static position; no screenshot tooling.

**WHAT TO BUILD**

1. **Persist modal state** — store modal open/closed, all form values (audio blob, transcription, screenshot URLs, category, text) in `localStorage` under `ideaBankDraft_${userId}` on every change; restore on mount; clear only on successful submit or explicit discard action.
2. **Draggable, resizable, collapsible modal** — modal becomes a floating window (no blanked backdrop, overlay with pointer-events-none except the window itself); draggable titlebar; resize handles (min 400×300, max 90vw×90vh); collapse button shrinks to animated bolt icon (small, stays in last corner, click re-expands); use the existing animated bolt SVG from the logo.
3. **Screenshot selector tool** — "Add Screenshot" button triggers browser screenshot API if available (fallback: prompt Lee to paste); on click, modal hides temporarily, display crosshair cursor + drag-to-select box; capture selected region, upload to `idea_screenshots` bucket, insert thumbnail in form, restore modal; backdrop stays semi-transparent so student sees what they're screenshotting.
4. **Update categories** — replace "Authoring" dropdown option with "Authoring/Editing"; add "Student Side" option; add "Other (specify)" with text input that appears when selected; remove subcategory field.
5. **Launch with animated bolt** — Control+I shows animated bolt in modal titlebar; titlebar says "⚡ Idea Bank" with bolt animation.
6. **Phased prompt option in submission** — after Lee submits, backend creates one `ideas` row with full JSON; if Lee checked "Split into phases" toggle (new field in modal), the row's `prompt` field wraps content with preamble: "Claude Code: ask Lee if he wants all phases at once or one at a time. Phases: 1) [first feature], 2) [second feature], etc." — no actual splitting logic, just prepend that instruction.

**WHERE**  
- Modal component: likely `app/components/IdeaBankModal.tsx` or inline in talkthrough route — find.
- Screenshot upload: `app/lib/supabase.server.ts` (storage helper) + new `idea_screenshots` bucket policy.
- Persistence: client-side localStorage hook, clear in submit action.
- Backend: `app/routes/api.ideas.submit.ts` or similar POST handler — find.

**ACCEPTANCE**

- Student fills modal, clicks away, reopens (Control+I), sees everything intact.
- Modal can be dragged, resized, collapsed to bolt, re-expanded without losing content.
- "Add Screenshot" dims modal, lets student drag a box, uploads region, shows thumbnail.
- Categories include Authoring/Editing, Student Side, Other (with text input).
- Animated bolt in titlebar; backdrop not blanked.
- "Split into phases" toggle; when checked, saved prompt includes phase instruction for Claude Code.
- Existing tests for idea submission still pass; no breakage on /v3 routes.

**OUT OF SCOPE**

- Actual phase splitting or Claude Code integration (just text instruction in prompt field).
- Browser screenshot API polyfill if unsupported (prompt paste fallback fine).
- Multi-idea management or editing past ideas (one draft at a time).
- Screenshot editing (crop/annotate) beyond basic selector.

**REPORT**

Per item: pass / fail / stubbed · SQL LEE MUST RUN (numbered migrations for `idea_screenshots` bucket policy if new table; otherwise NONE if only storage policy needed) · Ambiguous: "selector tool" — decided HTML5 drag box over frozen viewport screenshot (fallback: paste URL); "animated bolt" — reuse existing logo SVG, 2s spin on launch; "split into phases" — user toggle, backend just prepends instruction text, no logic.

## Testing checklist

- [ ] Navigate to `/v3/easy-points/internal-vs-external-users/blast-off/talkthrough`, press Control+I, fill title/audio/screenshot, click outside modal, press Control+I again — all fields restored.
- [ ] Drag modal by titlebar to new position, resize by corner, collapse to bolt icon in corner, click bolt to re-expand — state persists.
- [ ] Click "Add Screenshot," drag selector box over a UI element, release — screenshot uploads, thumbnail appears in modal.
- [ ] Select "Authoring/Editing" category, then "Other (specify)" — text input appears; select "Student Side" — no subcategory field shown.
- [ ] Check "Split into phases" toggle, submit idea, view saved prompt in database — includes "Claude Code: ask Lee…" instruction.
- [ ] Launch modal, see animated bolt in titlebar and semi-transparent backdrop (can see page content behind).
- [ ] Submit idea successfully, reopen modal — localStorage draft cleared, form empty.
- [ ] Run existing Bun tests for idea submission — all pass, no regressions.